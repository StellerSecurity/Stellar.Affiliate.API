@extends('layouts.affiliate')
@section('title', 'Payouts · Admin')
@section('content')
<section class="stellar-page-header"><div><p class="stellar-eyebrow">Financial operations</p><h1 class="stellar-page-title">Payouts</h1><p class="stellar-page-copy">Review payout status and payment references.</p></div></section>
<section class="stellar-card stellar-card-pad"><form class="stellar-filterbar" method="GET"><div class="stellar-field"><label class="stellar-label" for="payout-status-filter">Status</label><select id="payout-status-filter" class="stellar-select" name="status"><option value="">All</option>@foreach(['pending','processing','paid','failed'] as $option)<option value="{{ $option }}" {{ $statusFilter === $option ? 'selected' : '' }}>{{ ucfirst($option) }}</option>@endforeach</select></div><button type="submit" class="stellar-btn stellar-btn-primary">Filter</button></form></section>
<section class="stellar-card stellar-card-pad stellar-section"><div class="stellar-table-wrap"><table class="stellar-table"><thead><tr><th>Date</th><th>Affiliate</th><th>Amount</th><th>Method</th><th>Commissions</th><th>Reference</th><th>Status</th><th>Action</th></tr></thead><tbody>@forelse($payouts as $payout)<tr><td>{{ $payout->created_at?->format('M j, Y · H:i') }}</td><td>@if($payout->affiliate)<a class="stellar-text-link" href="{{ route('affiliate.admin.affiliates.show', $payout->affiliate) }}">{{ $payout->affiliate->public_code }}</a>@else<span>—</span>@endif</td><td class="strong">{{ $payout->currency }} {{ \App\Support\CommissionMath::display($payout->amount) }}</td><td>{{ ucfirst($payout->method_type ?: '—') }}</td><td>{{ number_format((int) ($payout->commissions_count ?? 0)) }}</td><td>{{ $payout->external_reference ?: '—' }}</td><td><span class="stellar-badge {{ $payout->status === 'paid' ? 'is-success' : ($payout->status === 'failed' ? 'is-danger' : 'is-warning') }}">{{ ucfirst($payout->status) }}</span></td><td>@if(auth()->user()?->canManageAffiliateCommissions())<form method="POST" action="{{ route('affiliate.admin.payouts.status', $payout) }}" class="stellar-inline-form">@csrf @method('PATCH')<select class="stellar-select stellar-compact-select" name="status" aria-label="Payout status">@foreach(['pending','processing','paid','failed'] as $option)<option value="{{ $option }}" {{ $payout->status === $option ? 'selected' : '' }}>{{ ucfirst($option) }}</option>@endforeach</select><input class="stellar-input stellar-ref-input" name="external_reference" aria-label="Payment reference" value="{{ $payout->external_reference }}" placeholder="Reference"><button type="submit" class="stellar-btn stellar-btn-secondary stellar-btn-small">Save</button></form>@else<span class="stellar-cell-sub">Read only</span>@endif</td></tr>@empty<tr><td colspan="8">No payouts found.</td></tr>@endforelse</tbody></table></div><div class="stellar-pagination">{{ $payouts->links() }}</div></section>
@endsection
